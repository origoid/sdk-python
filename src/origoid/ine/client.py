# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.envelope import Envelope
from .raw_client import AsyncRawIneClient, RawIneClient
from .types.validate_voter_list_request import ValidateVoterListRequest

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class IneClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawIneClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawIneClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawIneClient
        """
        return self._raw_client

    def validate_voter_list(
        self, *, request: ValidateVoterListRequest, request_options: typing.Optional[RequestOptions] = None
    ) -> Envelope:
        """
        **Credits:** 1 per call.

        Validates that a Mexican voter ID (INE / IFE) credential exists in INE's Lista Nominal — the official roll of registered voters — by sending CIC, OCR or ID number depending on the credential model. Returns a confirmation, the voter's polling section, and validity dates.

        Use this endpoint as part of KYC to verify that the voter ID presented by your customer is registered and valid (not stolen, not lost, not cancelled).

        Parameters
        ----------
        request : ValidateVoterListRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Envelope
            Business Responses (Strict Envelope Pattern).

        Examples
        --------
        from origoid import OrigoID

        client = OrigoID(
            api_key="YOUR_API_KEY",
        )
        client.ine.validate_voter_list(
            request={"key": "value"},
        )
        """
        _response = self._raw_client.validate_voter_list(request=request, request_options=request_options)
        return _response.data

    def extract_voter_id_data(
        self, *, front: str, back: typing.Optional[str] = OMIT, request_options: typing.Optional[RequestOptions] = None
    ) -> Envelope:
        """
        **Credits:** 1 per call.

        Performs OCR on the front and back of a Mexican voter ID (INE / IFE) and returns the structured data printed on the credential: full name, CURP, voter key (CIC / OCR), address, photograph metadata, the document model variant (D, E, F, G, H, I — current and recent INE designs), and the MRZ read from the back when present.

        What sets this endpoint apart is **integrated address normalization + geocoding**: the address printed on the INE is rarely clean — abbreviations, missing colonia, inconsistent casing. We normalize and enrich it automatically. You get back not only the raw address text, but also:

        - **`addressNormalized`**: the printed INE address, normalized and enriched (corrected casing, expanded abbreviations, validated postal code, and neighborhood / municipality / state matched from the official catalog). The `geocodingStatus` field reports the match confidence: `VERIFIED` (house- or street-level match), `PARTIAL` (locality or postal-code match), or `UNVERIFIED` (no confident match).
        - **`electoralGeography`**: derived electoral district, federal entity, and polling section — useful for cross-checking with `validateVoterList`.
        - **Document model detection** (D, E, F, G, H, I) and per-model security feature validation.
        - **MRZ + QR cross-validation**: when the back contains MRZ and QR, we read both and confirm they agree with the printed fields. Mismatches are flagged.

        Use this endpoint to digitize voter ID capture without manual transcription, and to obtain a geo-enriched address record in a single call — eliminating a separate geocoding step in your KYC flow.

        Parameters
        ----------
        front : str
            Front image in Base64 (PNG/JPG)

        back : typing.Optional[str]
            Back image in Base64 (PNG/JPG). Optional.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Envelope
            Extraction results (Strict Envelope Pattern).

        Examples
        --------
        from origoid import OrigoID

        client = OrigoID(
            api_key="YOUR_API_KEY",
        )
        client.ine.extract_voter_id_data(
            front="front",
        )
        """
        _response = self._raw_client.extract_voter_id_data(front=front, back=back, request_options=request_options)
        return _response.data

    def extract_qr_data(self, *, back: str, request_options: typing.Optional[RequestOptions] = None) -> Envelope:
        """
        **Credits:** 2 per call.

        Decrypts and parses the QR codes printed on Mexican voter IDs (INE models G, H, I and J). The two QRs on the back contain RSA-signed payloads with the holder's full record (name, CURP, voter key, address, signature). This endpoint decrypts both QRs and merges the result.

        Use this endpoint as a tamper-evidence check: if the QR decrypts successfully and matches the printed data, the credential is highly likely to be authentic.

        **Models I and J (2026+)** additionally carry a self-identified `gender` (may be `NB`), `selfIdentification` (autoadscripción, e.g. `INDÍGENA`), `ethnicGroup` (indigenous people), and the printed `address`. These are `null` on models G/H. `sex` (from the CURP) and `dateOfBirth` are present on all models.

        Parameters
        ----------
        back : str
            Back image of the INE credential in Base64 (PNG/JPG). The QR code must be visible. **Required.**

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Envelope
            Extraction results (Strict Envelope Pattern).

        Examples
        --------
        from origoid import OrigoID

        client = OrigoID(
            api_key="YOUR_API_KEY",
        )
        client.ine.extract_qr_data(
            back="back",
        )
        """
        _response = self._raw_client.extract_qr_data(back=back, request_options=request_options)
        return _response.data


class AsyncIneClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawIneClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawIneClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawIneClient
        """
        return self._raw_client

    async def validate_voter_list(
        self, *, request: ValidateVoterListRequest, request_options: typing.Optional[RequestOptions] = None
    ) -> Envelope:
        """
        **Credits:** 1 per call.

        Validates that a Mexican voter ID (INE / IFE) credential exists in INE's Lista Nominal — the official roll of registered voters — by sending CIC, OCR or ID number depending on the credential model. Returns a confirmation, the voter's polling section, and validity dates.

        Use this endpoint as part of KYC to verify that the voter ID presented by your customer is registered and valid (not stolen, not lost, not cancelled).

        Parameters
        ----------
        request : ValidateVoterListRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Envelope
            Business Responses (Strict Envelope Pattern).

        Examples
        --------
        import asyncio

        from origoid import AsyncOrigoID

        client = AsyncOrigoID(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.ine.validate_voter_list(
                request={"key": "value"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.validate_voter_list(request=request, request_options=request_options)
        return _response.data

    async def extract_voter_id_data(
        self, *, front: str, back: typing.Optional[str] = OMIT, request_options: typing.Optional[RequestOptions] = None
    ) -> Envelope:
        """
        **Credits:** 1 per call.

        Performs OCR on the front and back of a Mexican voter ID (INE / IFE) and returns the structured data printed on the credential: full name, CURP, voter key (CIC / OCR), address, photograph metadata, the document model variant (D, E, F, G, H, I — current and recent INE designs), and the MRZ read from the back when present.

        What sets this endpoint apart is **integrated address normalization + geocoding**: the address printed on the INE is rarely clean — abbreviations, missing colonia, inconsistent casing. We normalize and enrich it automatically. You get back not only the raw address text, but also:

        - **`addressNormalized`**: the printed INE address, normalized and enriched (corrected casing, expanded abbreviations, validated postal code, and neighborhood / municipality / state matched from the official catalog). The `geocodingStatus` field reports the match confidence: `VERIFIED` (house- or street-level match), `PARTIAL` (locality or postal-code match), or `UNVERIFIED` (no confident match).
        - **`electoralGeography`**: derived electoral district, federal entity, and polling section — useful for cross-checking with `validateVoterList`.
        - **Document model detection** (D, E, F, G, H, I) and per-model security feature validation.
        - **MRZ + QR cross-validation**: when the back contains MRZ and QR, we read both and confirm they agree with the printed fields. Mismatches are flagged.

        Use this endpoint to digitize voter ID capture without manual transcription, and to obtain a geo-enriched address record in a single call — eliminating a separate geocoding step in your KYC flow.

        Parameters
        ----------
        front : str
            Front image in Base64 (PNG/JPG)

        back : typing.Optional[str]
            Back image in Base64 (PNG/JPG). Optional.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Envelope
            Extraction results (Strict Envelope Pattern).

        Examples
        --------
        import asyncio

        from origoid import AsyncOrigoID

        client = AsyncOrigoID(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.ine.extract_voter_id_data(
                front="front",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.extract_voter_id_data(
            front=front, back=back, request_options=request_options
        )
        return _response.data

    async def extract_qr_data(self, *, back: str, request_options: typing.Optional[RequestOptions] = None) -> Envelope:
        """
        **Credits:** 2 per call.

        Decrypts and parses the QR codes printed on Mexican voter IDs (INE models G, H, I and J). The two QRs on the back contain RSA-signed payloads with the holder's full record (name, CURP, voter key, address, signature). This endpoint decrypts both QRs and merges the result.

        Use this endpoint as a tamper-evidence check: if the QR decrypts successfully and matches the printed data, the credential is highly likely to be authentic.

        **Models I and J (2026+)** additionally carry a self-identified `gender` (may be `NB`), `selfIdentification` (autoadscripción, e.g. `INDÍGENA`), `ethnicGroup` (indigenous people), and the printed `address`. These are `null` on models G/H. `sex` (from the CURP) and `dateOfBirth` are present on all models.

        Parameters
        ----------
        back : str
            Back image of the INE credential in Base64 (PNG/JPG). The QR code must be visible. **Required.**

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Envelope
            Extraction results (Strict Envelope Pattern).

        Examples
        --------
        import asyncio

        from origoid import AsyncOrigoID

        client = AsyncOrigoID(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.ine.extract_qr_data(
                back="back",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.extract_qr_data(back=back, request_options=request_options)
        return _response.data
